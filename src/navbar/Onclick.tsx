import React from 'react';

export const OnClick = () => {
  // Function to handle the button click
  return <>
     window.close();
  </>
   
    // Add more actions here if needed, e.g., close window, navigate, etc.
    // window.close(); // Use this if you want to try closing the window (may be blocked by the browser)




};


